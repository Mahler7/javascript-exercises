<!-- Calculates the future value of an investment using a for loop ... enhanced version-->

<script>
  var futureValue;
  var exit = "y"

  var yearStatement = "Year = ";
  var interestStatement = "Interest = ";
  var valueStatement = "Value = ";

  do { 
    var investment = prompt("Enter investment arount as xxxxx.xx", 10000);
    investment = parseFloat(investment);
    if (isNaN(investment)) {
      alert("Investment must be a number");
    }
    
    var rate = prompt("Enter interest rate as xx.x", 7.5);
    rate = parseFloat(rate);
    if (isNaN(rate) && rate > 0 && rate <= 15) {
      alert("Interest must be a number");
    }
 
    var years = prompt("Enter number of years", 10);
    years = parseInt(years);
    if (isNaN(years)) {
      alert("Years must be a number");
    }

    exit = prompt("Repeat entries?", "y");
  }
  while (exit == "y");

  futureValue = investment;
  for (var i = 1; i <= years; i++) {
    futureValue = futureValue + (futureValue * rate/100);
  }
  futureValue = parseInt(futureValue);

  document.write("Investment amount = " + investment);
  document.write(" Interest rate = " + rate);
  document.write(" Years = " + years);
  document.write(" Future value is " + futureValue + "<br><br>");
</script>